---
title: "TimeSeries2"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Work with git

```{console, eval=FALSE}
# sync with the latest version
# run this two lines of code in console
git fetch upstream
git merge upstream/master
```

```{r}
data = read.csv("B3_HWA2.csv", stringsAsFactors = FALSE)
data
```

